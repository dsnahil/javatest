class subset
{
    public static void main(String args[])
    {
    int Set[] = { 1, 2, 3 };
    int n = Set.length;
    if (n == 1)
    {
    System.out.print("XOR of XOR's of all subsets is "+ Set[0]);
}
else
    System.out.print("XOR of XOR's of all subsets is "+0);
    }
}